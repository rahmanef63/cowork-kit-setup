# This app needs NO environment variables to run.
#
# Optional: point the dashboard at a different project's datastore. When unset,
# the workspace is the parent of web/ (the project root), so the dashboard reads
# and writes the same .data/ and output/ folders as the CLI and the MCP server.
#
# AUTOMATION_WORKSPACE=
